# Port for the server to run on (Render handles this automatically in production)
PORT=5000

# Connection string for your PostgreSQL database (Render will provide this)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Cross-Origin Resource Sharing (Set to your frontend URL later)
CORS_ORIGIN="http://localhost:3000"